services:
  database:
    build:
      # nome cartella
      context: ./database
    ports:
      - "5050:5050"

  #gateway:
    #build:
      # nome cartella
      #context: ./gateway
    #ports:
      #- "6060:6060"

  firebase:
    build:
      # nome cartella
      context: ./firebase
    ports:
      - "7070:7070"

  chat:
    build:
      # nome cartella
      context: ./chat
    ports:
      - "3000:3000"

  #frontend:
    #build:
      # nome cartella
      #context: ./frontend
    #ports:
     # - "80:80"